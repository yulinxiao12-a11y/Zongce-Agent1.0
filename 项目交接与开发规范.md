# 综测星轨项目交接与开发规范

本文档用于记录项目开发期间已经明确的要求、协作规范和当前架构交接信息。切换对话或重新接手时，应先阅读本文档，再开始改动。

## Git 工作流要求

1. 每次更新或调整一个小功能后，都要及时提交一次 Git，避免大量改动堆在一起难以回滚。
2. 每次提交前，先向项目负责人推荐 2-3 个提交名，由项目负责人选择或自定义后再提交。
3. 每一块较大的功能都要先创建独立分支开发，不直接干扰 `main` 主干。
4. 大功能完成后，必须先完成必要测试和本地验收，再合并回 `main`。
5. 如果合并后发现效果不好，应优先回退该功能分支或重新开分支调整，不随意破坏主干历史。
6. 未明确要求时，不删除、不覆盖已有用户改动；遇到不确定的改动来源，先确认或绕开。

## 当前项目定位

项目以 `C:\Users\pwotyx\Desktop\综测星轨1.0` 为主仓库。

当前方向是保留 1.0 版本的 Vue3 前端架构、UI、配色和排版，同时接入源代码版本的 Flask 后端能力。源代码版本位于 `C:\Users\pwotyx\Desktop\综测星轨`，主要作为后端能力和旧 HTML 功能参考。

## 前端要求

1. 1.0 版本的 Vue3 架构、整体视觉风格、配色和排版要尽量保留。
2. 登录注册界面可以参考和移植源代码版本的样式。
3. 现在的 1.0 主界面作为管理端和学生端的统一 Vue 壳层继续使用。
4. 管理员登录后默认进入管理端，并可以切换学生端视角。
5. 学生端导航顺序要求：星盘总览、机会大厅、备赛清单、星轨探索、智审中心、荣誉星墙。
6. 管理端导航中，用户管理放在数据看板下面。
7. 管理端/学生端切换控件中，管理端在左，学生端在右。
8. 前端开发代理默认连接 Flask 后端 `http://127.0.0.1:5001`。

## 后端要求

1. 源代码版本 Flask 后端能力不能丢，尤其是登录注册、用户管理、材料提交、AI 审核、审核队列和管理端功能。
2. 源代码版本的 Jinja HTML 模板和旧静态资源保留作为功能兜底。
3. Vue 前端逐步迁移旧 HTML 功能，不要求一次性全部 Vue 化。
4. API 应使用 session cookie 鉴权，前端请求默认携带 `credentials: include`。
5. 未登录访问学生端或管理端应跳转登录页；学生不能访问管理端。

## AI 审核与材料识别要求

1. 智审中心应对应源代码版本的“材料提交”功能，不只是文件名匹配。
2. 后端必须能识别图片、截图、PDF、Word 等证明材料内容。
3. 图片证书、奖状、截图应通过 OCR 读取文字，再进行综测项目匹配。
4. PDF 应先抽取文本；如果文本不足，应将页面渲染为图片再 OCR，支持扫描版或截图型 PDF。
5. 学生端提交的综测申请必须进入管理端审核队列，数据看板也要同步统计。
6. AI 审核结果只能作为初审建议，最终入账以管理员审核或系统规则通过为准。

## 当前已验证的识别样例

1. `华为HCIA-AI.png`：能识别证书内容并匹配 `A046 专业技术等级证书（初级）`。
2. `HCIA认证证书.pdf`：能识别 PDF 内容并匹配 `A046 专业技术等级证书（初级）`。
3. `计算机二级.pdf`：能识别全国计算机等级考试二级证书并匹配 `A049 计算机等级考试（二级）`。
4. `肖毓潾_1702008889.jpg`：能识别蓝桥杯、C/C++、广东赛区、三等奖，并匹配 `A010 互联网+/大挑/小挑/电子设计竞赛（省级三等奖）`。

## 本地运行与验证

后端：

```powershell
cd C:\Users\pwotyx\Desktop\综测星轨1.0\webapp
C:\Users\pwotyx\Desktop\综测星轨\webapp\.venv\Scripts\python.exe -m flask --app app run --host 127.0.0.1 --port 5001
```

前端：

```powershell
cd C:\Users\pwotyx\Desktop\综测星轨1.0\frontend
npm run dev -- --host 127.0.0.1
```

常用检查：

```powershell
py -3.12 -m py_compile webapp\app.py webapp\ai_engine.py
cd frontend; npm run build
git status --short --branch
```

## 注意事项

1. 不要把 AI 密钥、私密配置或 `.env` 内容写入代码和日志。
2. `upstream-reference/` 是未跟踪的只读参考目录，不要删除或随意修改。
3. 旧 HTML 页面作为兜底功能保留；Vue 迁移时逐页替换。
4. 每次改动后优先做最小必要验证；涉及 AI 审核、上传、审核队列时必须做端到端验证。
